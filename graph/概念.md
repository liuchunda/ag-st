# 节点 node 图中的基本计算单元
# 边 edge 节点之间的链接（有先后）关系，以及数据流向
# GraphState 图的状态，可以理解为图的数据，是一个字典 可序列化
# seng 某些情况下，节点的出边是未知的，需要在节点中根据数据的类型和数量来决定流转到一个或多个节点，或者同一个节点分别对不同的数据执行一遍任务
# command 是一个用于控制图执行的多功能原语
# config 设定图的可配置内容 比如用来切换模型，设置模型参数，设置系统提示词