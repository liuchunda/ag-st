"""
3检索类型对比.py —— IVF_FLAT / HNSW / IVF_PQ 等（人选哪种加速）

前提都一样：表里有真实向量；索引是额外加速结构；目标是 ANN（又快又尽量准）。
差别只在「目录长什么样、搜时怎么走」。


════════════════════════════════════════════════════════════
共用小例子
════════════════════════════════════════════════════════════
库里有很多行，其中一行：

  原文: 「我是工程师，叫张三，21岁」
  向量: v_张三 = [0.12, -0.05, ...]   # 假设 128 维

问题: 「谁是21岁工程师？」→ q = embed(问题)

下面三种索引，都是为了少算距离、尽快摸到张三附近。


════════════════════════════════════════════════════════════
1. FLAT（精确扫，几乎「不建花活」）
════════════════════════════════════════════════════════════
结构：基本没有近似目录，就是向量列表。
搜索：q 和每一条向量算距离，取 top-k。

例：10 万条就比 10 万次。
  优点：最准（召回可视为满分）
  缺点：数据一大就慢
  何时用：数据很少、或要当「正确答案基准」做对比


════════════════════════════════════════════════════════════
2. IVF_FLAT（分桶 + 桶内原向量精比）← 见 3创建索引.py
════════════════════════════════════════════════════════════
结构：
  · k-means 得到 nlist 个桶中心
  · 每条向量进一个桶；桶里仍存「完整原向量」

搜索：
  ① q 比全部桶中心
  ② 进最近的 nprobe 个桶
  ③ 只在这些桶里用完整向量精比 → top-k

例：nlist=128，nprobe=16
  不是扫 10 万，只扫「16 个桶里的几千条」。
  若张三在桶37，且桶37中心离 q 够近、进了前16 → 能找到
  若没进这16桶 → 漏（召回↓）

  优点：实现直观，桶内仍精确
  缺点：桶里向量仍占满内存/带宽；极大规范时不如带压缩的


════════════════════════════════════════════════════════════
3. IVF_PQ（分桶 + 向量压缩） 
════════════════════════════════════════════════════════════
PQ = Product Quantization（乘积量化）

和 IVF_FLAT 一样先分桶；差别在桶里存的不是完整 float 向量，
而是「压缩码」（把高维向量拆段、每段用码本近似表示）。

形象：
  IVF_FLAT：桶里放高清原图
  IVF_PQ  ：桶里放压缩缩略图（省空间、比得快），必要时再回原图精修

搜索直觉：
  ① 仍先靠桶中心选 nprobe 个桶
  ② 在桶里用压缩码快速估距离（很便宜）
  ③ 常再对少数候选用原向量精排（看实现/配置）

例：同样找到张三，但中间大量比较是在「压缩空间」里完成。
  优点：更省内存、大数据更快
  缺点：多一层近似 → 召回/精度通常不如 IVF_FLAT；调参更多


════════════════════════════════════════════════════════════
4. HNSW（多层近邻图）← 见 3HNSW索引.py；all.py/Chroma 常见
════════════════════════════════════════════════════════════
结构：每个向量是点；相近点连边；分上下层（高速→小路）。
搜索：从上层往离 q 近的邻居跳，层层下钻，到底层取 top-k。

例：张三连着其他「工程师」点；q 跳几步落到这片 → 取出张三。
  没有 nlist/nprobe；常见参数 M、ef（查询看多宽）。

  优点：高召回、延迟稳，内存型场景很常用
  缺点：图占内存；构建比简单 IVF 重


════════════════════════════════════════════════════════════
5. 一张对照表
════════════════════════════════════════════════════════════
类型        核心结构           搜时关键步              准/快/省直觉
---------   ----------------   -------------------   -----------------
FLAT        无/弱索引          全量比                最准 / 最慢 / —
IVF_FLAT    桶+原向量          选桶→桶内精比         较准 / 快 / 一般
IVF_PQ      桶+压缩码          选桶→压缩估距(±精排)  更省更快 / 更近似
HNSW        多层近邻图         图上往近处跳          高召回稳 / 吃内存

都是人选的 index_type（或框架默认替你选）：
  Milvus:  "index_type": "IVF_FLAT" | "IVF_PQ" | "HNSW" | "FLAT" ...
  Chroma:  默认常走 HNSW 一类，业务侧多半只写 query


════════════════════════════════════════════════════════════
6. 怎么选（学习用规则）
════════════════════════════════════════════════════════════
· 先搞懂流程、数据又少     → FLAT 或 IVF_FLAT（概念最清晰）
· 要分桶直觉               → IVF_FLAT（3创建索引.py）
· 数据很大、内存紧         → IVF_PQ（速度/空间优先）
· 要高召回、内存够         → HNSW（all.py 这条线）

调参共性：放宽搜索（更大 nprobe / 更大 ef）→ 召回↑、速度↓。


════════════════════════════════════════════════════════════
7. 代码长相（只看差别）
════════════════════════════════════════════════════════════
# IVF_FLAT
{"metric_type": "L2", "index_type": "IVF_FLAT",
 "params": {"nlist": 128}}
# search param: {"nprobe": 16}

# IVF_PQ（m/nbits 控制压缩粒度，细节因库而异）
{"metric_type": "L2", "index_type": "IVF_PQ",
 "params": {"nlist": 128, "m": 8, "nbits": 8}}
# search param: {"nprobe": 16}

# HNSW
{"metric_type": "L2", "index_type": "HNSW",
 "params": {"M": 16, "efConstruction": 200}}
# search param: {"ef": 64}

# FLAT
{"metric_type": "L2", "index_type": "FLAT", "params": {}}
"""

print("检索类型对比笔记：IVF_FLAT / IVF_PQ / HNSW / FLAT。细节见 3创建索引.py 与 3HNSW索引.py。")
