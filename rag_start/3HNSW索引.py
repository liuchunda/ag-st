"""
3HNSW索引.py —— HNSW 近邻图核心笔记（对照 IVF 看）

════════════════════════════════════════════════════════════
0. 和 IVF 同一起点
════════════════════════════════════════════════════════════
表里仍是真实数据：

  id | 原文「我是工程师，叫张三，21岁」 | embedding=[0.12, -0.05, ...]

索引仍是额外加速结构（真实占内存/磁盘），不是原文本身。
统称都是 ANN：近似最近邻，用一点精度换速度。


════════════════════════════════════════════════════════════
1. HNSW 是什么（形象）
════════════════════════════════════════════════════════════
全称：Hierarchical Navigable Small World（分层可导航小世界图）

把每条向量当成图上一个「点」，相近的点连「边」。
还分好几层：

  上层：点少，像高速公路 —— 跳得远，粗定位
  下层：点多，像小路 —— 在附近精细逛

类比导航：
  IVF  = 先查小区门牌，再进几栋楼挨家找（分桶）
  HNSW = 不先分小区，顺着路牌一层层靠近目的地（在图上跳）


════════════════════════════════════════════════════════════
2. 建索引时在算什么
════════════════════════════════════════════════════════════
IVF：k-means → 桶中心 +「谁在哪个桶」
HNSW：给每个点找近邻、连边，并分配到若干层

插入张三时（直觉）：
  · 在图上找到一批离他近的点（其他工程师）连上边
  · 不太去连「厨师」那些远点
  · 上层少连几条「远跳」边，下层连得更密

没有「128 个桶中心」；结构是「点 + 邻居指针」。


════════════════════════════════════════════════════════════
3. 搜索时怎么走（对照 IVF）
════════════════════════════════════════════════════════════
问题：「谁是21岁工程师？」→ 也变成向量 q

IVF：
  q 比全部桶中心 → 进 nprobe 个桶 → 桶内精比 → top-k

HNSW：
  ① 从上层某个入口出发
  ② 看当前点的邻居，往「离 q 更近」的方向跳
  ③ 本层逛不动了，下到更细的一层继续跳
  ④ 到底层后，在附近取出最近的几条 → 拿 id 取原文

张三会被找到，是因为图上「工程师」一带互相连着，
q 跳几步容易落到张三附近——不是因为进了「桶37」。


════════════════════════════════════════════════════════════
4. 参数直觉（不必一次记死）
════════════════════════════════════════════════════════════
IVF 你已见过：
  nlist  = 建多少桶
  nprobe = 搜时探几个桶（大→召回高、慢）

HNSW 常见：
  M     ≈ 每个点连多少邻居（图多密）
  efConstruction ≈ 建图时搜多宽（建得慢但图更好）
  ef / efSearch  ≈ 查询时候选看多宽（大→召回高、慢）

和 nprobe 同类权衡：看得越宽，越不易漏，越慢。


════════════════════════════════════════════════════════════
5. 对照表（记住即可）
════════════════════════════════════════════════════════════
          IVF                         HNSW
结构      桶 + 桶中心                   点 + 近邻边（多层）
建索引    k-means 分桶                  连边、分层
搜索      选桶再扫                      在图上往近处跳
手写感    create_index + nprobe         常藏在框架里
代表      Milvus 示例见 3创建索引.py   Chroma / all.py 默认常见


════════════════════════════════════════════════════════════
6. all.py 里你怎么「碰到」HNSW
════════════════════════════════════════════════════════════
你不写 create_index / load。
add 写入向量时，Chroma 在内部维护近邻图；
query 时在图上跳着找近邻：

  collection.query(query_embeddings=[q], n_results=3)

所以：不是「没有索引全表 for 循环」，
而是「索引是 HNSW 图，框架自动建、自动用」。
若漏检，调的是图搜索宽度一类参数，不是 nprobe（那是 IVF 的）。
"""

# ---------------------------------------------------------------------------
# 对照示意（不必跑）：Milvus 也可显式建 HNSW；Chroma 则通常自动
# ---------------------------------------------------------------------------
#
# # Milvus 显式（字段仍叫 embedding，只是 index_type 换成 HNSW）
# index_params = {
#     "metric_type": "L2",
#     "index_type": "HNSW",
#     "params": {"M": 16, "efConstruction": 200},
# }
# collection.create_index("embedding", index_params)
# collection.load()
# collection.search(
#     data=[q],
#     anns_field="embedding",
#     param={"metric_type": "L2", "params": {"ef": 64}},  # 查询宽度 ≈ 召回↔速度
#     limit=3,
# )
#
# # Chroma / all.py（自动 HNSW，业务代码只看见这一句）
# collection.query(query_embeddings=[q], n_results=3)

print("本文件是 HNSW 学习笔记；可运行检索见 all.py 的 retrieve/query。")
