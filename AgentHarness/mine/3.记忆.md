
.memory

压缩会丢细节，要有一些不会丢的。可以跨压缩跨会话

MEMORY.md文件目录文件，廉价可读。记录简介和索引
记录信息：名称，简介，类型，文件名称

学习
提取细节，新建文件保存到文件中

目录
加入memory.md文件索引

召回
匹配目录，提取文件，读取内容详情，发给大模型

记忆插入顺序。压缩完之后，发送大模型之前，插入记忆
插入记忆：让大模型选择最相关的5条记忆，插入上下文

# 每次大模型结束之后提取记忆文件

记忆整理，去重，合并，剪枝

# 关键设计
索引常驻system prompt 可以被prompt cache缓存
写入分两条路径，用户显示说记住  或者每轮结束后后台提取，文件多了进行整理

# 大模型进来先加载与对话相关的记忆
注入提示词在压缩之前
要新建一个记忆列表。备份消息列表，避免备份的消息被压缩

大模型结束之后提取记忆文pre_compress=[]
大模型调用之后，没有工具调用了，要结束了，这个时候提取记忆
extract_messages(messages)
获取所有记忆文件
逆序便利消息列表，提取最近三条用户消息
获取索引内容
prompt=(
    根据近期对话和目录索引，返回相关的记忆索引
    近期对话内容：“”
    索引内容：“”
)
调用大模型找相似的列表
得到选中的文件列表
获取内容，添加到列表中
- 加到提示词后边，不影响前边的系统提示词命中缓存
兜底：如果大模型没找到，把消息按空格分开，大于3个字的列入关键字列表
看看关键字是不是在记忆的内容中，记忆的内容包括「name]+content
也可以用jieba分词




第一步
提取前10条消息
第二步
# 把文件排序 保证顺序稳定，容易命中缓存
列出所有的记忆文件，包含所以记忆文件的元数据内容
第三步
创建提示词
files.stem 不包含扩展名的文件名
prompt=(
    "从对话中提取用户偏好，约束项目事实"
    "返回json数组，每项{name,type,description,body}"
    "-name :短kebab-case标识"
    =descriotion:行摘要供索引"
    "-body:markdown详情"
    "若无新记忆或已被现有记忆覆盖，返回[]"
    f"现有记忆：existing_desc 对话内容：dialogue"

)
dialogue="\n".join([f"{role}:{text}"])
existing_desc ="\n".join([f"{name}:{description }"])

调用大模型，提取记忆
写入记忆文件，更新索引






# 整理加固记忆
consolidate_memories():
判断文件的数量数不是大于阈值
大于阈值的话进行整理
构建所用记忆内容的目录文本，并用于合并提示

prompt=（
“合并以下记忆文件，规则”
1.重复项合并为1条
2.删除过时的记忆，矛盾的选最新的【这里的过时是被新的习惯覆盖】
3.总条数控制在30条
4.优先保留最终要的
返回数组name，type，ndesctions，body

）
返回结果。
删除所有记忆文件，然后重新写入

有些agent还有一些记忆半衰期，记忆会随着时间指数衰减，越久远的记忆影响越小


存疑 整理agent记忆时，每次删除了重新写，会不会消化资源和危险