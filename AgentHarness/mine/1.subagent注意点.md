生命周期，上下文传递，上下文截断策略，权限管理，结果处理，重生与恢复
1。subagent运行时，主进程崩溃，怎么保证子进程不变成孤儿进程

父进程的字段怎么防止泄口给子进程

上下文8k，你截断式，为什么有限保留工具调用的结果，而不是用户的最新问答


subagent注入攻击，诱导调用deleteall工具，怎么防止

多个子agent分别返回结果，父agent怎么裁决



拆分提示词
拆分工具
basetool


# 怎么防止子代理循环起子代理
- 在子代理的提示词里
- 子代理的工具里没有代理工具

子工具调用，写在executor里，没有写在handlers中。防止循环引用



subagent本质是一个工具，返回结果，通过tool结果返回


/btw指令，不是开启独立子进程，而是在当前回话中，创建一个轻量的临时链，复用主ahgent上下文和提示词缓存，但是回答的记过不会写入主对话历史，实现零污染并进行回答。子agent是新回话记录