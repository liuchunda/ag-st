状态机：等待，就绪认领，执行，完成

有向无环图，避免死循环，可排序为线性序列

canstart blockedby 全部completed


todowrite，存在内存，生命周期会话结束就消失，如责任无，状态校验enum检查

taskSystem，写入硬盘，生命周期跨会话持久，依赖blockedby，owner字段agent，状态claim/complete有前置条件

复杂任务往往拆成多步。没有持久任务图，agent只能凭上下文记住，上下文压缩或者重启，任务就丢失了。需要持久话任务存储

 添加系统提示词：

 schema添加工具：5个
 1.添加任务工具create_task
 创建新任务，可选blockedby
 _fn_tool(
    "create_task",
    "",
    {
        "subject":{"type":"string"},
        "description":{"type":"string"},
        "blockedBy":{"type":"array","item":{"type":"string"}},
    },
    ["subject"],
 )
2.列出所有的任务状态，负责人与依赖【list_tasks】
3.按任务ia获取任务完整详情【get_task】
4.认领pedding状态的任务，设置owner并把状态改为inprogress【claim_task】
5.完成状态为in_progress的任务，并报告下游解阻塞的任务【complete_task】

五个阶段，创建任务，任务列表，获取详情byid，认领任务，完成任务解锁下游

阻塞任务id列表

把任务写进硬盘【本地文件】
def create_task(subject,description,blockedBy:list[str])
    task = Task(
        id:task_(time.time)
        subject:subject,
        description:description,
        blockedBy:blockedBy
    )

list_task

load_task //传入taskid返回task实例

get_task//把实例获取字符串

claim_task//认领task。如果任务的状态不是pedding无法认领，如果任务被阻塞无法认领，通过task.blockedby和task.status。判断依赖的任务和任务状态

complete_task_id //如果任务是in_progress，则完成
便利所有任务列表，找到因本任务完成可以解锁的可以开始的任务，高所已解锁的任务列表

完成某个任务后，并没有更新其他依赖任务的状态，只改自己的状态，看其他任务对自己的依赖状态变化了 ，只是跟大模型说了一句
有哪些任务可以认领执行了
执行过程中不能删除任务，都完成后可以把整套删除

delete_task,删除已完成的任务。判断这个任务还有被其他任务依赖吗，如果其他依赖他的任务已经完成，可以删除，否则不能删除。删除时清理依赖他的任务清除依赖 unlink


