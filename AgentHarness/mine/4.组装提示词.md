identity 负责模块core 你是一个编程助手
tools工具注册表：可用工具xxx
workspace负责模块运行时：当前工作区/xxx
memory+skills负责模块：上下文加载器加载记忆索引和技能

关键设计，section是否加载取决于真实状态【工具是否存在，记忆文件是否存在】，不是消息里的关键词

# 系统提示词缓存机制
每次修改系统提示词，维护全局变量last_memory_mtime。获取时判断记忆文件修改时间是不是等于这个时间
判断全局变量 last_prompt是否存在，并且last_memory_mtime和记忆索引文件修改时间一样，直接取last_prompt
如果变化了，就重新去组装