# 工具调用结果占用空间很大，优先压缩工具结果

最大一般100k
三种触发方式，微调，自动，手动

微调
压缩冗余输出【旧的tool消息】
自动
摘要整段对话【令牌阈值】
手动 /compact
手动仅保留一条压缩摘要【用户调用命令】

五种压缩策略，l5 大模型返回工具过程api报错，触发反向压缩【应急裁剪】

核心设计原则：便宜的先跑，贵的后跑
能改文本，不删整条。能删整条，不调大模型

上下文管线。预处理压缩，自动压缩，应急兜底


压缩插入在调用LLM之前，三种触发模型
四层压缩策略
触发机制：每轮自动压缩无条件，每轮触发0api
l3
tool_result_budget
工具结果预算，压缩

l1
snip——compact
消息裁剪

l2micro——compact
微压缩 工具结果变成摘要 如果摘要之后还是超阈值


l4 压缩历史，把历史消息压成一条摘要。要调用LLMapi 全量摘要



压缩策略

# 调用大模型之前
第一层
L3：tool_result_budget函数
history.py
参数messages maxbytes
筛选出所有role是tool的，获取整体的消息计算出大小
如果没有超过大小，直接返回messages

否则，按工具消息内容长度进行排列。最大的在前边
然后判断每条工具内容的大小，如果超过持久化的值，则落盘到硬盘落盘目录【.tash_ouputs/tool_results】。并替换为简短的信息【完整信息的存放目录+例如前2000个字符】。覆盖完之后，再算一次整体消息的大小总和
persist_large_output(tool_id,content)

第二层
L1:snip_compact 消息数大于50【经验值】条，裁掉中间，保留头部尾部 头部3条，尾部47条[最新的]
修复消息链，确保assitant的toolcall和too的响应对应。同时移除孤立 的tool消息
repair_message_chain(messages)
缺少工具调用结果，补全【此消息被剪裁了，已修复】
缺少工具调用消息，把工具结果删掉
如果不是成对出现，大模型会报错，消息不匹配，恢复的消息必须是对前边消息的响应

第三层
L2 micro_compact 旧工具占位，仅保留最近3条tool的完整内容，旧的变成占位符
例如有10条工具调用结果
只保留后三条的，前七条的，内容替换为【较早的工具结果已经压缩，需要时重新运行】

第四层
LLM全量摘要
L4 compact_history
触发条件，前三层处理完了，估算消息 
token数>contexWindow-maxOutputTokens-13000
如果触发了条件，先尝试sessionMemoryCompact(用已有记忆做轻量摘要)。不足时才调用LLM
步骤1.保存transcript，对话写入jsonl格式的文件 【transcript_(timestamp).jsonl】
步骤2.LL生成摘要
把对话历史发给LLM，摘要包含9部分
请求概念，文件错误，解决，用户消息代办，当前下一步，只生成一次
步骤3.替换消息列表，所有旧消息替换为一条摘要
发送之前要可以根据模型能力，进行截取，一般80000字符
压缩prompt=（
    "总结以下agent对话，以便继续工作"
    "保留1.当前目标，2.关键发现决策，3.读和改过的文件4.剩余工作5.用户约束\n简介但具体"+json.dumps(message,default=str,ensure=Flase)[:80000]
）
调用LLM压缩
压缩返回数据summary
{
    role:"user",
    content:"[已压缩]summary"
}

压缩后，
autocompact失败3次，停止重试。防止上下文反复调用api浪费。

四部压缩之后开始调用大模型


# 大模型调用。发送消息之后
try:
    response = call_llm(system,messages,maxtokens,model)
except Execption as e:
    if(is_prompt_too_long_error(e))
        messages[:]=reactive_compact(message)
        continue

reactive_compact
prompt_tool_long(413)上下文增长速度快于压缩触发速度

转录文件write_transcripts(messages)
生成一个压缩后的消息链：保留消息列表中，最后的5条消息
messages[-5:] 然后修复工具链 repair_message_chian



# 用户主动压缩上下文compact工具。调用reactive_compact
添加系统提示词，上下文过长可以调用这个用具reactive_compact。

如果用户调用了压缩工具，那压缩之后，跳出工具循环的调用。跳到下一轮大模型循环

message[:]=list
原地替换
print(id(messages))不变节约内存

# jsonl文件，json lines 一种每行一个独立的json对象的格式。多行之间用\n分割
  

保留索引，命中缓存

# 四类记忆
你是谁user 怎么做事feedback project正在发生什么 reference东西在哪找

记忆的选择时大模型选择的 、

# 两种加载记忆路径
路径一，索引常驻系统提示词

路径二
相关记忆按需注入上下文，每轮调用之前，load_memories（）
把最近对话和记忆目录一起发给大模型，做一次轻量silde-query 选出相关的文件名，再读取内容注入上下文，最多5条。控制开销
如果silde-query失败，会降级到关键词匹配

写入：每轮结束后提取

#  记忆适合保存什么
用户偏好，反复出现的反馈，项目背景，常用入口，排查线索